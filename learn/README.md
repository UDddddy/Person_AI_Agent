# 学习脚本

早期学习阶段的实验脚本，从项目根目录移至此，不参与生产运行。

| 文件 | 说明 | 可运行 |
|---|---|---|
| `learn_graph.py` | LangGraph 最小图练习（State/Node/Edge/条件边），不依赖项目内部模块 | ✅ |
| `learn_sse.py` | SSE 流式输出练习，依赖已删除的 `app.llm`（阶段1旧 client） | ❌ 仅历史参考 |
